# Hash Tables

Implementation of hash tables in C.

## Description

A hash table is a data structure that maps keys to values using a hash function. This project implements a hash table using chaining (linked lists) to handle collisions.

## Files

- `hash_tables.h` - Header file with struct definitions and prototypes
- `0-hash_table_create.c` - Creates a new hash table 
